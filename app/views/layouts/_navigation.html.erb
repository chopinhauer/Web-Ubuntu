<nav class="navbar navbar-expand-lg navbar-light bg-light affix  " id="navbar_font">
          <%= link_to "My Blog", root_path, class: "navbar-brand", id: "logo" %>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
              <li class="nav-item active">
                <%= link_to "About", about_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Bloggers", users_path, class: "nav-link" %>
              </li>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Categories
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <% if logged_in? && current_user.admin? %>
                    <%= link_to "Create New Category", new_category_path, class: "dropdown-item" %>
                  <% end %>
                      <% Category.all.each do |category| %>
                        <%= link_to category.name, category_path(category), class: "dropdown-item text-capitalize" %>
                      <%end%>

                </div>
              </li>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Articles
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <% if logged_in? %>
                    <%= link_to "Create New Article", new_article_path, class: "dropdown-item" %>
                  <% end %>
                    <%= link_to "Listing All Articles", articles_path, class: "dropdown-item" %>
                  <div class="dropdown-divider"></div>
                  <a class="dropdown-item" href="#"> </a>
                </div>
              </li>
            </ul>

            <% if logged_in? %>
              <ul class="nav navbar-nav  navbar-right  ">
                  <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle " href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="fa fa-user" aria-hidden="true"></i><span class="ml-2">User</span>
                  </a>
                  <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <div class="dropdown-header">
                          <%= "(ADMİN)" if current_user.admin? %> User:  <%= current_user.username.capitalize %>
                        </div>
                      <div class="dropdown-divider"></div>
                        <%= link_to "View your profile", user_path(current_user), class: "dropdown-item" %>
                        <%= link_to "Edit your profile", edit_user_path(current_user), class: "dropdown-item" %>
                  </div>
                </li>
                <li class="nav-item" >
                  <a class="nav-link text-decoration-none">
                    <i class="fa fa-lg fa-sign-out" aria-hidden="true"></i>
                    <%= link_to "Logout", logout_path, class: "nav-link-user", method: :delete %>
                  </a>
                </li>
              </ul>
                <% else %>
                  <ul class="nav navbar-nav  navbar-right  ">
                    <li class="nav-item" >
                      <a class="nav-link text-decoration-none">
                        <i class="fa fa-lg fa-sign-in" aria-hidden="true"></i>
                        <%= link_to " Log in ", login_path, class: "nav-link-user" %>
                      </a>
                    </li>
                    <li class="nav-item ml-2" >
                      <a class="nav-link text-decoration-none">
                        <i class="fa  fa-user-plus" aria-hidden="true"></i>
                        <%= link_to "Sign up ", signup_path, class: "nav-link-user " %>
                      </a>
                    </li>
                  </ul>
                <% end %>
          </div>
  </div>
</nav>
